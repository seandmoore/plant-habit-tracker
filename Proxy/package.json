{
  "name": "plant-companion-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "sync:catalog": "node scripts/sync-catalog.mjs",
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
